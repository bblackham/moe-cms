Evaluator
~~~~~~~~~
box: konfigurovatelny pristup k timerum a /proc/self/stat (pripadne /proc/self/fd)
score: better formatting of the score table
interactive tasks: solve deadlocks?
interactive tasks: logging of messages

Environment
~~~~~~~~~~~
fpc: add switch -gl (runtime errors write line#)
check versions of g++

Distribution:
~~~~~~~~~~~~~
- ssh keys
- kernel sources
- remove cups
- /aux/mo/public/bin/submit -- missing x-bit
- STL docs
- ? distribuovatko testingu do home directories
- pripsat do guide.tex, ze se rhide nema spoustet z mc
- rhide: backslashe na konci radku v Pascalu -> strange compilation error
- problemy s gpm po nastaveni fontu
